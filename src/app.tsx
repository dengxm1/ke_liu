import { history } from 'umi';
/**
 * 运行时配置-用于优化未登录打开页面跳屏
 * @param oldRender 旧的渲染
 */

/*export function render(oldRender) {
  fetch('/api/users').then(auth => {
    console.log('auth', auth);
    if (1) {
      oldRender();
    } else {
      history.push('/test');
    }
  });
}*/
